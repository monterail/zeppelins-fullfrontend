pre-commit:
  parallel: true
  commands:
    eslint:
      run: yarn eslint --ext .ts,.js,.vue --fix {staged_files} && git add {staged_files}
    format:
      run: yarn prettier --ignore-unknown --ignore-path .gitignore {staged_files} && git add {staged_files}
